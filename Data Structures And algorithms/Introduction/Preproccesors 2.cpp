#include <iostream>
#include <fstream>
#include <cmath>
#include <bits/stdc++.h>
#define max(x, y) (x > y ? x : y)
#define msg(x) #x
#define add(x, y) (x + y)
//Important thing we cannot pass variables in these functions
using namespace std;

int main()
{
    int r = 10, y = 20;
    cout << "hello" << endl;
    cout << max(10, 20);

    return 0;
}